import placeSlice from './placeSlice';
import courseSlice from './courseSlice';
import tagSlice from './tagSlice';
import authSlice from './authSlice';

export {placeSlice, courseSlice, tagSlice, authSlice};
